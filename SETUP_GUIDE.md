# 🚀 네이버 부동산 봇 실행 가이드

완전 초보자도 따라할 수 있는 단계별 실행 가이드입니다.

---

## 📋 목차
1. [사전 준비](#1-사전-준비)
2. [텔레그램 봇 생성](#2-텔레그램-봇-생성)
3. [지역 코드 찾기](#3-지역-코드-찾기)
4. [로컬 환경 설정](#4-로컬-환경-설정)
5. [로컬 테스트 실행](#5-로컬-테스트-실행)
6. [GitHub 설정 (자동화)](#6-github-설정-자동화)
7. [필터 조정](#7-필터-조정)
8. [문제 해결](#8-문제-해결)

---

## 1. 사전 준비

### ✅ 필요한 것들
- [ ] Python 3.11 이상 설치
- [ ] Git 설치
- [ ] 텔레그램 앱 (모바일/데스크톱)
- [ ] GitHub 계정
- [ ] 인터넷 연결

### Python 설치 확인
```powershell
python --version
```
출력 예시: `Python 3.11.0` 또는 그 이상

**없다면:** https://www.python.org/downloads/ 에서 다운로드

### Git 설치 확인
```powershell
git --version
```
출력 예시: `git version 2.40.0`

**없다면:** https://git-scm.com/downloads 에서 다운로드

---

## 2. 텔레그램 봇 생성

### Step 1: BotFather와 대화 시작

1. 텔레그램 앱 실행
2. 검색창에 `@BotFather` 입력
3. 공식 BotFather 선택 (파란색 체크마크 확인)
4. **START** 버튼 클릭

### Step 2: 봇 생성

1. `/newbot` 입력 후 전송
2. 봇 이름 입력 (예: `내 부동산 알림봇`)
3. 봇 사용자명 입력 (예: `my_realestate_bot`)
   - 반드시 `bot`으로 끝나야 함
   - 영문, 숫자, 언더스코어만 가능

### Step 3: 토큰 저장

봇이 생성되면 다음과 같은 메시지를 받습니다:

```
Done! Congratulations on your new bot.
Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-123456

Keep your token secure and store it safely, it can be used by anyone to control your bot.
```

**🔴 중요: 이 토큰을 안전한 곳에 복사해두세요!**

형식: `숫자:영문숫자하이픈 조합`

### Step 4: 채팅 ID 확인

1. 생성한 봇과 대화 시작
   - BotFather가 보낸 링크 클릭 (예: `t.me/my_realestate_bot`)
   - 또는 검색창에서 봇 사용자명 검색
   
2. **START** 버튼 클릭 (또는 `/start` 입력)

3. 아무 메시지나 전송 (예: "안녕")

4. 웹 브라우저에서 다음 주소 접속:
   ```
   https://api.telegram.org/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-123456/getUpdates
   ```
   ⚠️ `bot` 다음에 위에서 받은 토큰을 붙여넣으세요

5. 결과에서 채팅 ID 찾기:
   ```json
   {
     "ok": true,
     "result": [
       {
         "update_id": 123456789,
         "message": {
           "message_id": 1,
           "from": {...},
           "chat": {
             "id": 987654321,    ← 이 숫자가 채팅 ID!
             "first_name": "홍길동",
             "type": "private"
           },
           "date": 1234567890,
           "text": "안녕"
         }
       }
     ]
   }
   ```

**🔴 중요: 이 채팅 ID를 복사해두세요!**

형식: `숫자만 (보통 9~10자리)`

---

## 3. 지역 코드 찾기

### Step 1: 네이버 부동산 접속

1. 브라우저에서 https://new.land.naver.com 접속

### Step 2: 원하는 지역 검색

1. 검색창에 원하는 지역 입력 (예: "강남구 대치동")
2. 아파트 또는 오피스텔 선택

### Step 3: URL에서 지역 코드 복사

URL 예시:
```
https://new.land.naver.com/complexes?cortarNo=1168010600&tradTpCd=...
```

`cortarNo=` 뒤의 숫자가 **지역 코드**입니다.

**예시:**
- `1168010600`: 강남구 대치동
- `1165010100`: 서초구 반포동
- `1168010700`: 강남구 도곡동
- `1111010100`: 종로구 청운효자동

**🔴 중요: 원하는 지역들의 코드를 모두 복사해두세요!**

여러 지역 가능: `1168010600,1165010100,1168010700`

---

## 4. 로컬 환경 설정

### Step 1: 프로젝트 폴더로 이동

PowerShell 실행 후:

```powershell
cd "c:\Users\BYEONGWOOK MIN\naver-realestate-bot"
```

### Step 2: 가상환경 생성

```powershell
python -m venv venv
```

실행 시간: 약 10~30초

### Step 3: 가상환경 활성화

**PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
```

**⚠️ 실행 정책 오류 발생 시:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
그 후 다시 활성화 명령어 실행

**성공 확인:**
프롬프트 앞에 `(venv)` 표시가 나타남
```
(venv) PS C:\Users\BYEONGWOOK MIN\naver-realestate-bot>
```

### Step 4: 패키지 설치

```powershell
pip install -r requirements.txt
```

설치되는 패키지:
- requests (API 호출)
- python-telegram-bot (텔레그램 알림)
- python-dotenv (환경 변수 관리)
- selenium, undetected-chromedriver (웹 크롤링)

실행 시간: 약 1~3분

**성공 확인:**
```
Successfully installed requests-2.31.0 python-telegram-bot-20.7 ...
```

### Step 5: .env 파일 생성

**방법 1: PowerShell 명령어로 생성**
```powershell
@"
TELEGRAM_BOT_TOKEN=여기에_실제_토큰_입력
TELEGRAM_CHAT_ID=여기에_실제_채팅ID_입력
SEARCH_REGIONS=1168010600
TRADE_TYPES=A1,B1
"@ | Out-File -FilePath .env -Encoding UTF8
```

**방법 2: 메모장으로 수동 생성**

1. 메모장 실행
2. 다음 내용 입력:
   ```env
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-123456
   TELEGRAM_CHAT_ID=987654321
   SEARCH_REGIONS=1168010600
   TRADE_TYPES=A1,B1
   ```
3. "다른 이름으로 저장" → 파일 이름: `.env` → 파일 형식: **모든 파일**
4. 저장 위치: `c:\Users\BYEONGWOOK MIN\naver-realestate-bot`

### Step 6: .env 파일 수정

메모장으로 `.env` 파일을 열고 실제 값으로 변경:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-123456
TELEGRAM_CHAT_ID=987654321
SEARCH_REGIONS=1168010600,1165010100
TRADE_TYPES=A1,B1
```

**설정 값 설명:**
- `TELEGRAM_BOT_TOKEN`: 2단계에서 받은 토큰
- `TELEGRAM_CHAT_ID`: 2단계에서 확인한 채팅 ID
- `SEARCH_REGIONS`: 3단계에서 찾은 지역 코드 (쉼표로 구분)
- `TRADE_TYPES`: 
  - `A1`: 매매
  - `B1`: 전세
  - `B2`: 월세
  - `B3`: 단기임대

### Step 7: data 폴더 생성 (선택)

```powershell
mkdir data
```

⚠️ 코드 실행 시 자동으로 생성되므로 생략 가능

---

## 5. 로컬 테스트 실행

### Test 1: 기본 모듈 테스트 (환경 변수 불필요)

```powershell
cd src
python test_basic.py
```

**예상 결과:**
```
============================================================
  기본 모듈 테스트 시작
============================================================

1. 모듈 Import 테스트
------------------------------------------------------------
[OK] database.py import 성공
[OK] scraper.py import 성공
[OK] filter_manager.py import 성공
[OK] telegram_bot.py import 성공

2. 설정 파일 로드 테스트
------------------------------------------------------------
[OK] filters.json 로드 성공

3. 데이터베이스 초기화 테스트
------------------------------------------------------------
[OK] 데이터베이스 초기화 성공
[OK] 테스트 매물 추가 성공

4. 필터링 로직 테스트
------------------------------------------------------------
[OK] 필터 통과 테스트 성공
[OK] 필터 차단 테스트 성공

5. 프로젝트 파일 구조 확인
------------------------------------------------------------
[OK] ../config/filters.json
[OK] ../requirements.txt
...

============================================================
테스트 결과 요약
============================================================

총 테스트: 5개
[OK] 성공: 5개
[FAIL] 실패: 0개

============================================================
축하합니다! 모든 기본 테스트 통과!
============================================================
```

**❌ 실패 시:** 실패 항목 확인 후 8번 "문제 해결" 참고

### Test 2: 통합 모듈 테스트

```powershell
python test_modules.py
```

이 테스트도 환경 변수 없이 실행 가능합니다.

### Test 3: 텔레그램 연결 테스트

`.env` 파일이 올바르게 설정되었는지 확인:

**방법 1: 브라우저로 테스트**
```
https://api.telegram.org/bot<YOUR_TOKEN>/sendMessage?chat_id=<YOUR_CHAT_ID>&text=테스트메시지
```

**방법 2: Python으로 테스트**
```powershell
python -c "from telegram_bot import TelegramNotifierSync; from dotenv import load_dotenv; load_dotenv('../.env'); bot = TelegramNotifierSync(); bot.send_message('테스트 성공!')"
```

**✅ 성공:** 텔레그램에서 메시지 수신

### Test 4: 실제 크롤링 테스트 (테스트용 필터)

⚠️ 본격 실행 전에 필터를 느슨하게 설정하세요!

**config/filters.json 임시 수정:**
```json
{
  "property_types": ["APT", "OPST"],
  "trade_types": ["A1"],
  "price_range": {
    "A1": {"min": 0, "max": 999999}
  },
  "area_range": {"min": 0, "max": 999999},
  "approval_year": {"min": 0, "max": 9999},
  "household_count": {"min": 0, "max": 999999},
  "floor_types": [],
  "room_count": [],
  "bathroom_count": [],
  "directions": [],
  "loan": "상관없음",
  "options": []
}
```

**실행:**
```powershell
python main.py
```

**예상 결과:**
```
============================================================
부동산 크롤링 시작
실행 시간: 2026-01-28 15:30:00
============================================================

[지역 크롤링] cortarNo: 1168010600
=== 거래 유형 A1 크롤링 시작 ===
단지 검색: cortarNo=1168010600, tradeType=A1
검색된 단지 수: 50
[1/10] 래미안 대치팰리스 (complexNo: 1234)
검색된 매물 수: 15
필터 통과: 15개 매물
신규 매물 발견: 래미안 대치팰리스 - 1234_67890
알림 전송 완료
...

============================================================
크롤링 완료 요약
============================================================
전체 크롤링 매물: 150개
필터 통과 매물: 150개
신규 매물: 150개
알림 전송: 150개

[데이터베이스 통계]
총 저장 매물: 150개
알림 완료: 150개
알림 대기: 0개

============================================================
모든 작업 완료
============================================================
```

**✅ 성공 확인:**
1. 콘솔에 매물 정보 출력
2. 텔레그램으로 알림 수신
3. `data/properties.db` 파일 생성
4. `scraper.log` 파일 생성

**⚠️ 너무 많은 알림이 올 경우:**
- `Ctrl + C`로 중단
- 7번 "필터 조정"으로 이동

---

## 6. GitHub 설정 (자동화)

### Step 1: GitHub 저장소 생성

1. https://github.com 접속
2. 우측 상단 **+** → **New repository**
3. 저장소 이름: `naver-realestate-bot`
4. **Public** 선택 (GitHub Actions 무료)
5. **Create repository**

### Step 2: 로컬 코드 업로드

```powershell
# 프로젝트 루트로 이동
cd "c:\Users\BYEONGWOOK MIN\naver-realestate-bot"

# Git 초기화 (이미 되어있다면 생략)
git init

# 모든 파일 추가 (.env는 자동으로 제외됨)
git add .

# 커밋
git commit -m "Initial commit: 네이버 부동산 크롤링 봇"

# 원격 저장소 연결
git remote add origin https://github.com/YOUR_USERNAME/naver-realestate-bot.git

# 업로드
git branch -M main
git push -u origin main
```

**⚠️ 로그인 필요:**
- GitHub 사용자명과 Personal Access Token 입력
- Token 생성: Settings → Developer settings → Personal access tokens → Generate new token

### Step 3: GitHub Secrets 설정

1. GitHub 저장소 페이지로 이동
2. **Settings** 탭 클릭
3. 좌측 메뉴: **Secrets and variables** → **Actions**
4. **New repository secret** 클릭
5. 다음 4개의 Secret 추가:

| Name | Value | 예시 |
|------|-------|------|
| `TELEGRAM_BOT_TOKEN` | 봇 토큰 | `1234567890:ABCdefGHI...` |
| `TELEGRAM_CHAT_ID` | 채팅 ID | `987654321` |
| `SEARCH_REGIONS` | 지역 코드 | `1168010600,1165010100` |
| `TRADE_TYPES` | 거래 유형 | `A1,B1` |

**각 Secret 추가 방법:**
1. Name에 위 이름 입력
2. Value에 실제 값 입력
3. **Add secret** 클릭
4. 4개 모두 반복

### Step 4: GitHub Actions 활성화 확인

1. 저장소의 **Actions** 탭 클릭
2. "네이버 부동산 크롤링" 워크플로우 확인
3. 2시간마다 자동 실행됨

### Step 5: 수동 테스트 실행

1. **Actions** 탭
2. "네이버 부동산 크롤링" 클릭
3. **Run workflow** → **Run workflow** 클릭
4. 워크플로우 실행 확인 (약 2~5분 소요)

**✅ 성공 확인:**
- 모든 단계에 초록색 체크마크
- 텔레그램으로 알림 수신
- Artifacts에 로그 파일 저장

**❌ 실패 시:**
- 빨간색 X 표시 클릭
- 로그 확인
- Secrets 값 재확인

### Step 6: 자동 실행 스케줄 확인

`.github/workflows/scraper.yml` 파일 확인:

```yaml
on:
  schedule:
    - cron: '0 */2 * * *'  # 2시간마다 실행
```

**실행 시간:**
- UTC 기준 2시간마다
- 한국 시간: 00:00, 02:00, 04:00, 06:00, 08:00, 10:00, 12:00, 14:00, 16:00, 18:00, 20:00, 22:00

**스케줄 변경하려면:**
```yaml
- cron: '0 */3 * * *'  # 3시간마다
- cron: '0 9,18 * * *'  # 매일 09:00, 18:00 (UTC)
- cron: '0 0,12 * * *'  # 매일 00:00, 12:00 (UTC)
```

---

## 7. 필터 조정

### 현재 필터 (매우 엄격)

`config/filters.json`:
```json
{
  "trade_types": ["A1", "B1"],
  "price_range": {
    "A1": {"min": 50000, "max": 150000},
    "B1": {"min": 30000, "max": 80000}
  },
  "area_range": {"min": 59, "max": 84},
  "approval_year": {"min": 2018, "max": 2024},
  "household_count": {"min": 300, "max": 2000},
  "floor_types": ["중간층", "고층"],
  "room_count": [2, 3],
  "bathroom_count": [2],
  "directions": ["남향", "남동향", "남서향"],
  "loan": "상관없음"
}
```

### 추천 설정 1: 가격만 제한 (매물 많음)

```json
{
  "trade_types": ["A1", "B1"],
  "price_range": {
    "A1": {"min": 50000, "max": 200000},
    "B1": {"min": 30000, "max": 100000}
  },
  "area_range": {"min": 0, "max": 999999},
  "approval_year": {"min": 0, "max": 9999},
  "household_count": {"min": 0, "max": 999999},
  "floor_types": [],
  "room_count": [],
  "bathroom_count": [],
  "directions": [],
  "loan": "상관없음"
}
```

### 추천 설정 2: 균형잡힌 설정

```json
{
  "trade_types": ["A1", "B1"],
  "price_range": {
    "A1": {"min": 50000, "max": 150000},
    "B1": {"min": 30000, "max": 80000}
  },
  "area_range": {"min": 59, "max": 99},
  "approval_year": {"min": 2015, "max": 2026},
  "household_count": {"min": 200, "max": 999999},
  "floor_types": ["중간층", "고층"],
  "room_count": [],
  "bathroom_count": [],
  "directions": [],
  "loan": "상관없음"
}
```

### 필터 수정 후 적용

**로컬:**
```powershell
cd src
python main.py
```

**GitHub Actions:**
1. `config/filters.json` 수정
2. Git 커밋 & 푸시:
   ```powershell
   git add config/filters.json
   git commit -m "Update: 필터 설정 조정"
   git push
   ```
3. 다음 스케줄 실행 시 자동 반영

---

## 8. 문제 해결

### 🔴 문제 1: PowerShell 실행 정책 오류

**증상:**
```
.\venv\Scripts\Activate.ps1 cannot be loaded because running scripts is disabled
```

**해결:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 🔴 문제 2: pip install 실패

**증상:**
```
ERROR: Could not find a version that satisfies the requirement...
```

**해결:**
```powershell
python -m pip install --upgrade pip
pip install -r requirements.txt
```

### 🔴 문제 3: 텔레그램 메시지 안 옴

**원인 1: 봇과 대화를 시작하지 않음**
- 봇을 찾아서 **START** 버튼 클릭

**원인 2: 채팅 ID 오류**
- getUpdates로 다시 확인
- 숫자만 입력 (따옴표 없이)

**원인 3: 토큰 오류**
- BotFather에서 토큰 재확인
- 공백 없이 복사

**테스트:**
```
https://api.telegram.org/bot<TOKEN>/sendMessage?chat_id=<CHAT_ID>&text=test
```

### 🔴 문제 4: 403 Forbidden 에러

**증상:**
```
접근 거부 (403). 헤더 변경 후 재시도...
```

**원인:** 네이버 API 일시적 차단

**해결:**
1. 잠시 후 재시도 (10~30분)
2. VPN 사용
3. GitHub Actions에서 실행 (IP가 다름)

### 🔴 문제 5: 매물이 하나도 안 나옴

**원인:** 필터가 너무 엄격

**해결:**
1. `config/filters.json`을 "추천 설정 1"로 변경
2. 하나씩 조건 추가하면서 테스트

### 🔴 문제 6: ModuleNotFoundError

**증상:**
```
ModuleNotFoundError: No module named 'requests'
```

**원인:** 가상환경이 활성화되지 않음

**해결:**
```powershell
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 🔴 문제 7: .env 파일을 찾을 수 없음

**증상:**
```
텔레그램 봇 초기화 실패: 텔레그램 봇 토큰과 채팅 ID가 필요합니다.
```

**원인:** .env 파일이 없거나 위치가 잘못됨

**확인:**
```powershell
ls .env
```

**해결:**
1. 프로젝트 루트에 `.env` 파일 생성
2. 파일 이름에 점(.) 포함 확인
3. 파일 형식: 모든 파일 (*.*) 선택

### 🔴 문제 8: GitHub Actions 실패

**증상:** Actions 탭에서 빨간색 X 표시

**확인 방법:**
1. Actions 탭 클릭
2. 실패한 워크플로우 클릭
3. 실패한 단계 클릭
4. 로그 확인

**주요 원인:**
1. **Secrets 미설정** → Settings에서 4개 Secret 추가
2. **Secrets 값 오류** → 값 재확인 (공백 제거)
3. **의존성 설치 실패** → requirements.txt 확인

### 🔴 문제 9: 데이터베이스 오류

**증상:**
```
sqlite3.OperationalError: unable to open database file
```

**해결:**
```powershell
mkdir data
```

### 🔴 문제 10: 너무 많은 알림

**해결:**
1. `Ctrl + C`로 중단
2. `data/properties.db` 삭제:
   ```powershell
   rm data/properties.db
   ```
3. 필터 조정 후 재실행

---

## 🎉 성공 확인 체크리스트

### 로컬 실행
- [ ] test_basic.py 모두 통과
- [ ] test_modules.py 모두 통과
- [ ] 텔레그램 테스트 메시지 수신
- [ ] main.py 실행 성공
- [ ] 텔레그램으로 매물 알림 수신
- [ ] data/properties.db 파일 생성
- [ ] scraper.log 파일 생성

### GitHub Actions
- [ ] 저장소 생성 완료
- [ ] 코드 푸시 완료
- [ ] 4개 Secrets 설정 완료
- [ ] 수동 실행 테스트 성공
- [ ] 텔레그램으로 알림 수신
- [ ] Actions 탭에서 초록색 체크마크 확인

---

## 📞 추가 도움이 필요한 경우

1. **GitHub Issues:** https://github.com/YOUR_USERNAME/naver-realestate-bot/issues
2. **로그 확인:**
   - 로컬: `scraper.log`
   - GitHub: Actions 탭 → 워크플로우 → 로그 다운로드
3. **텔레그램 API 문서:** https://core.telegram.org/bots/api
4. **네이버 부동산:** https://new.land.naver.com

---

## 🚀 다음 단계

1. **필터 최적화:** 원하는 매물만 받도록 조정
2. **지역 추가:** SEARCH_REGIONS에 여러 지역 코드 추가
3. **알림 확인:** 2시간마다 자동으로 알림 수신
4. **데이터 분석:** properties.db 파일로 매물 추적

**축하합니다! 🎊 이제 자동 부동산 알림 봇이 작동합니다!**
